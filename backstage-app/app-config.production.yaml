app:
  # Should be the same as backend.baseUrl when using the `app-backend` plugin.
  # Set APP_BASE_URL env var to your server's public URL, e.g. http://192.168.1.10:7007
  baseUrl: ${APP_BASE_URL}

backend:
  # APP_BASE_URL must be reachable by browsers and other clients, not just localhost.
  baseUrl: ${APP_BASE_URL}
  # Bind to all interfaces so the container port is accessible.
  listen: ':7007'
  cors:
    origin: ${APP_BASE_URL}
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true

  # config options: https://node-postgres.com/apis/client
  database:
    client: pg
    connection:
      host: ${POSTGRES_HOST}
      port: ${POSTGRES_PORT}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      # https://node-postgres.com/features/ssl
      # you can set the sslmode configuration option via the `PGSSLMODE` environment variable
      # see https://www.postgresql.org/docs/current/libpq-ssl.html Table 33.1. SSL Mode Descriptions (e.g. require)
      # ssl:
      #   ca: # if you have a CA file and want to verify it you can uncomment this section
      #     $file: <file-path>/ca/server.crt

auth:
  providers:
    guest: {}



catalog:
  locations:
    - type: file
      target: ./examples/entities.yaml
    - type: file
      target: ./examples/template/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ./examples/org.yaml
      rules:
        - allow: [User, Group]
    # ── k9 (TechDocs) ──
    - type: url
      target: https://github.com/willsreistech/k9/blob/main/catalog-info.yaml





