# Infra BackstageIO (Terraform)

Este diretório será usado para provisionar:

- Cluster (EKS/GKE/AKS ou VM)
- Banco PostgreSQL gerenciado
- Storage e networking
- Deploy do container gerado pelo Dockerfile

Por enquanto, o foco é ambiente local (Vagrant + Docker).
